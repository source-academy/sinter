{
  "private": true,
  "name": "sinter-wasm",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "scripts": {
    "build": "preact build --no-prerender",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^7.9.0",
    "eslint-config-preact": "^1.1.1",
    "preact-cli": "=3.0.1"
  },
  "dependencies": {
    "file-saver": "^2.0.2",
    "js-slang": "^0.4.64",
    "preact": "^10.4.8",
    "react-ace": "^9.1.3"
  }
}
